/* OleVariant := WideString operator stub.
 * In its own .o file so the linker only pulls it when not already
 * defined by compiler-emitted FPC Pascal code. */
char *olevariant__op_assign_widestring_u(char *source)
{
    return source;
}
