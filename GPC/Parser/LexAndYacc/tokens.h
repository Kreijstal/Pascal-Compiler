#ifndef TOKENS_H
#define TOKENS_H

typedef enum {
    TOKEN_EOF = 0,
    // Add other token definitions here as needed
    // This should match the token definitions from Grammar.y
} Token;

#endif