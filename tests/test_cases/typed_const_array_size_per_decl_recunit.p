unit typed_const_array_size_per_decl_recunit;

interface

type
  trec2 = packed record
    u  : word;
    c1 : byte;
    c2 : byte;
  end;

procedure dummy(p : pointer);

implementation

procedure dummy(p : pointer);
begin
end;

end.
